# Single root requirements: install all Python deps for Controller, Database, Webscraping.
# Use one .venv at project root: python3 -m venv .venv && source .venv/bin/activate
# Then: pip install -r requirements.txt

-r Controller/requirements.txt
-r Database_Model_Connection/requirements.txt
-r Webscraping/requirements.txt
